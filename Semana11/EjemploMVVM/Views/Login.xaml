<Window x:Class="EjemploMVVM.Login"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:EjemploMVVM"
        mc:Ignorable="d"
        Title="Login" Height="450" Width="800">
    <Grid Margin="30">
        <Grid VerticalAlignment="Center">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>
            <StackPanel Grid.Row="0" Orientation="Horizontal" VerticalAlignment="Center" HorizontalAlignment="Center" Margin="0,0,0,10">
                <TextBlock Text="Usuario:" Width="100"/>
                <TextBox Text="{Binding usuario}" Width="250"/>
            </StackPanel>
            <StackPanel Grid.Row="1" Orientation="Horizontal" VerticalAlignment="Center" HorizontalAlignment="Center" Margin="0,0,0,7">
                <TextBlock Text="Contraseña:" Width="100"/>
                <TextBox Text="{Binding password}" Width="250"/>
            </StackPanel>
            <StackPanel Grid.Row="2">
                <Button Padding="5,5,5,5" Content="Iniciar Sesión" Width="100" Command="{Binding LoginCommand}" />
            </StackPanel>
        </Grid>


    </Grid>
</Window>
