<Window x:Class="EnlaceDatos.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:EnlaceDatos"
        mc:Ignorable="d"
        Title="MainWindow" Height="450" Width="800">
    <Grid>
        <StackPanel Margin="20">
            <TextBox TextWrapping="Wrap" Text="{Binding Nombre}" FontSize="15" Margin="0,10,0,10"/>
            <TextBox TextWrapping="Wrap" Text="{Binding Nombre, Mode=OneWay}" FontSize="15" Margin="0,10,0,10"/>
            <TextBlock TextWrapping="Wrap" Text="{Binding Nombre}"/>
            <TextBox TextWrapping="Wrap" Text="{Binding Precio, Mode=OneWayToSource}"/>
            <Button x:Name="btnMostrar" Content="Button" Click="btnMostrar_Click"/>
        </StackPanel>

    </Grid>
</Window>
