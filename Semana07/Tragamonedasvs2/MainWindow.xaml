<Window x:Class="Tragamonedasvs2.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Tragamonedasvs2"
        mc:Ignorable="d"
         Title="Tragamonedas_v2" Height="600" Width="900" MinWidth="600" MinHeight="400">
    <Grid Background="White" Margin="12">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Encabezado: reloj grande centrado -->
        <Border Grid.Row="0" HorizontalAlignment="Center" Padding="18" Margin="0,8,0,8" Background="#e0fff6" CornerRadius="4">
            <Viewbox Stretch="Uniform" MaxHeight="120">
                <TextBlock x:Name="TimerText" Text="19:18:55" FontSize="48" FontWeight="Bold" Foreground="#0b0b0b" FontFamily="Segoe UI"/>
            </Viewbox>
        </Border>

        <!-- Botón iniciar centrado -->
        <StackPanel Grid.Row="1" HorizontalAlignment="Center" Margin="0,6">
            <Button x:Name="StartButton" Content="Iniciar Juego" Padding="10,6" MinWidth="120" MinHeight="30" Click="StartButton_Click"/>
        </StackPanel>

        <!-- Área principal: imágenes a la izquierda, puntaje a la derecha -->
        <Grid Grid.Row="2" Margin="8,16">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="220"/>
            </Grid.ColumnDefinitions>

            <!-- Imágenes centradas y responsivas -->
            <Grid Grid.Column="0">
                <Grid.RowDefinitions>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>
                <Viewbox Stretch="Uniform" Margin="10" HorizontalAlignment="Center" VerticalAlignment="Center">
                    <Grid Width="720" Height="200">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>

                        <!-- Cada casilla es un recuadro morado con un placeholder para la imagen -->
                        <Border Grid.Column="0" Background="#2f0f4f" Margin="8" CornerRadius="6" HorizontalAlignment="Stretch" VerticalAlignment="Stretch">
                            <Image x:Name="Img1" Stretch="Uniform" Margin="12" Source="/imagen/1.png" />
                        </Border>

                        <Border Grid.Column="1" Background="#2f0f4f" Margin="8" CornerRadius="6" HorizontalAlignment="Stretch" VerticalAlignment="Stretch">
                            <Image x:Name="Img2" Stretch="Uniform" Margin="12" Source="/imagen/2.png" />
                        </Border>

                        <Border Grid.Column="2" Background="#2f0f4f" Margin="8" CornerRadius="6" HorizontalAlignment="Stretch" VerticalAlignment="Stretch">
                            <Image x:Name="Img3" Stretch="Uniform" Margin="12" Source="/imagen/3.png" />
                        </Border>
                    </Grid>
                </Viewbox>
            </Grid>

            <!-- Panel de puntaje -->
            <StackPanel Grid.Column="1" HorizontalAlignment="Center" VerticalAlignment="Center">
                <TextBlock Text="Puntaje:" FontWeight="SemiBold" FontSize="16" Margin="8" HorizontalAlignment="Center"/>
                <Border Background="#ffec7a" Width="72" Height="72" CornerRadius="6" HorizontalAlignment="Center" VerticalAlignment="Center">
                    <TextBlock x:Name="ScoreText" Text="10" FontSize="28" FontWeight="Bold" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                </Border>
            </StackPanel>
        </Grid>

        <!-- Mensaje inferior -->
        <Border Grid.Row="3" Background="#e0fff6" CornerRadius="3" Padding="12" Margin="30,12,30,12" HorizontalAlignment="Stretch">
            <TextBlock x:Name="ResultText" Text="PERDISTE, puntaje obtenido: 10" FontWeight="Bold" FontSize="16" Foreground="#0d2b5b" TextAlignment="Center"/>
        </Border>
        </Grid>
</Window>
